name: Flatpak CI

on:
  push:
    paths:
      - '.github/actions/flatpak/action.yml'
      - '.github/workflows/io.github.tdesktop_x64.TDesktop.yml'
      - 'io.github.tdesktop_x64.TDesktop/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-24.04
    env:
      APP: io.github.tdesktop_x64.TDesktop
      GPG_KEY_ID: ${{ secrets.FLATPAK_GPG_KEY_ID }}
    steps:
      - name: Checkout source code
        uses: actions/checkout@v5
        with:
          submodules: recursive
      - name: Build Flatpak package
        uses: ./.github/actions/flatpak
        with:
          gpg-key: ${{ secrets.FLATPAK_GPG_KEY }}
          rclone-config: ${{ secrets.FLATPAK_RCLONE_CONFIG }}
